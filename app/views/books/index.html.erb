<h1>Books</h1>
<% @books.each do |book| %>
<%= book.title %>
<%= book.body %>
<% end %>

<h2>New Book</h2>
<%= form_with model: @book do |f| %>
  <%= f.label :Title %>
  <%= f.text_field :title %>
  <%= f.label :Body %>
  <%= f.text_area :body %>
  <%= f.submit '投稿' %>
<% end %>